import 'package:flutter/material.dart';

/// Couleurs partagées par tout JobMatch — le bleu reste l'accent principal,
/// homogène avec le reste de l'app (Explorer, Profil...), même si la
/// maquette fournie pour cette refonte utilisait du vert. Seul le vert de
/// l'action "Intéressé"/like est conservé (convention déjà présente avant
/// cette refonte : cœur vert = like). Rouge (Passer) et ambre
/// (Sauvegarder) inchangés.
const jobMatchAccent = Color(0xFF3B82F6);
const jobMatchLike = Color(0xFF16A34A);
